FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn[standard] python-multipart anthropic
COPY admin_app.py .
CMD ["uvicorn", "admin_app:app", "--host", "0.0.0.0", "--port", "8001"]
