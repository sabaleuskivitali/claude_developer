Запусти релиз сервера через официальный скрипт:

```bash
~/Applications/Claude/Dev/release server "$ARGUMENTS"
```

Не делай git команды вручную. Только через этот скрипт — он сам определит версию по тегу, закоммитит, смёрджит в main, запушит и создаст тег.
