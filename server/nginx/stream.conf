stream {
    # TCP proxy: external port 443 → API on internal port 49200
    # TLS is handled by the API itself (self-signed cert in /certs)
    server {
        listen 443;
        proxy_pass 127.0.0.1:49200;
        proxy_connect_timeout 5s;
        proxy_timeout 600s;
    }
}
